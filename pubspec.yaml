name: bonalume_popoff
description: "App per la gestione della centralina Bonalume."
publish_to: 'none'

version: 1.50.0+250

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  url_launcher: ^6.2.5
  shared_preferences: ^2.2.2
  flutter_reactive_ble: ^5.3.1
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/logo_bonalume.png
